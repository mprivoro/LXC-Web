# LXC-Web Python dependencies (Python 3.8+)
#   pip3 install -r requirements.txt
#
# Stdlib is not listed (argparse, sqlite3, subprocess, configparser, …).
# Host LXC tools (`lxc-*`) are not a pip package — install via apt.

Flask>=2.0
flask-sock>=0.5
simple-websocket>=0.10
ptyprocess>=0.7
# MCP server (python3 lwp.py starts it next to the panel). Skip on 3.8/3.9.
mcp>=2.0; python_version >= "3.10"
